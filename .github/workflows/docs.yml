name: Deploy Docs 

on: 
  repository_dispatch:
    types: [deploy-docs, deploy-evolve-docs]

jobs:
  deploy-docs:
    env:
      GITHUB_TOKEN: ${{ secrets.CI_GITHUB_TOKEN }}
    runs-on: ubuntu-latest
    steps:

      - uses: actions/checkout@v4
        with:
          token: ${{ env.GITHUB_TOKEN }}

      - name: Validate and parse parameters
        id: parse
        shell: bash
        run: |
          if [[ "${{github.event.client_payload.product}}" != "evolve" && "${{github.event.client_payload.product}}" != "ednar" ]]; then
            echo "Unsupported product deployment requested '${{github.event.client_payload.product}}'! Only 'ednar' or 'evolve' supported."
            echo ":boom: Unsupported product deployment requested '${{github.event.client_payload.product}}'! Only 'ednar' or 'evolve' supported." >> "${GITHUB_STEP_SUMMARY}"
            exit 1
          fi

          component=$(echo ${{github.event.client_payload.download_url}} | cut -f2 -d\/)
          echo "component=$component" >> "${GITHUB_ENV}"

          echo
          echo "Will attempt to deploy a component '$component' under 'zepben.github.io/${{github.event.client_payload.product}}' using repo '${{github.event.client_payload.download_url}}'" 
          echo


      - name: Download the snapshot release
        if: contains(github.event.client_payload.download_url, 'zepben')
        run: |
          # Sleep to make sure that the workflow that generated the snapshot has finished and the artifact is available 
          sleep 10s
          # download latest artifact from the other repository 
          echo "Download the artifact from ${{github.event.client_payload.download_url}}"
          gh run download -p docs.zip -R ${{github.event.client_payload.download_url}} `gh run list -R ${{github.event.client_payload.download_url}} -L1 --json headBranch,databaseId | jq '.[].databaseId'` 

          # gh download for the artifact is a bit dumb, so move things around
          mv docs.zip snapshot
          mv snapshot/docs.zip .
          rm -rf snapshot
        shell: bash

      # TODO: remove
      - name: Download doc site from GitHub
        if: contains(github.event.client_payload.download_url, 'github.com')
        run: |
          echo ":boom: This step shouldn't be used! Talk to Alex."
          exit 1
          # url=${{ github.event.client_payload.download_url }}
          # IFS='/' read -ra url_path <<< ${url/"https://github.com/"/}
          # echo "REPO=${url_path[0]}/${url_path[1]}" >> $GITHUB_ENV
          # echo "TAG=${url_path[4]}" >> $GITHUB_ENV
          # echo "DOCS_FILE_NAME=${url_path[5]}" >> $GITHUB_ENV
        shell: bash

      # TODO: remove
      - name: use vars
        if: contains(github.event.client_payload.download_url, 'github.com')
        run: |
          echo ":boom: This step shouldn't be used! Talk to Alex."
          exit 1
          # echo ${{ env.REPO }}
          # echo ${{ env.TAG }}
          # echo ${{ env.DOCS_FILE_NAME }}
        shell: bash

      # TODO: remove
      # Following the 2 sections above, we should not use this flow anymore.
      # - uses: dsaltares/fetch-gh-release-asset@master
      #   if: contains(github.event.client_payload.download_url, 'github.com')
      #   with:
      #     repo: "${{ env.REPO }}"
      #     version: "tags/${{ env.TAG }}"
      #     file: "${{ env.DOCS_FILE_NAME }}"
      #     target: "docs.zip"
      #     token: ${{ secrets.CI_GITHUB_TOKEN }}

      - name: Replace docs site with the downloaded one
        run: |
          docs_path="./${{ github.event.client_payload.product }}/docs/$component"
          rm -rf "$docs_path"
          mkdir "$docs_path"
          unzip docs.zip -d "$docs_path"
          git add "${docs_path}/*"
          staged_files=$(git diff --staged --name-only "${docs_path}")
          if [[ ! -z "$staged_files" ]]; then
            git config user.email "ci.github@zepben.com"
            git config user.name "Github CI"
            git commit -m "Updated site for $component"
            git push origin master
          else
            echo "There are no changed to deployment, if this is wrong, talk with Alex"
          fi
        shell: bash

      - name: Put out nice info
        run: |
          echo "✅ Another successful deployment: the \`$component\` was deployed under \`zepben.github.io/${{github.event.client_payload.product}}\`" >> "${GITHUB_STEP_SUMMARY}"
