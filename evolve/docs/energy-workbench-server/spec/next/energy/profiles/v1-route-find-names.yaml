get:
  tags:
    - Energy Profiles v1
  summary: Find network assets.
  description: >-
    DEPRECATED, use the network find API instead.


    Find network assets that match the search value.
  operationId: energyFindNames
  deprecated: true
  parameters:
    - $ref: 'v1-parameters.yaml#/searchParam'
    - $ref: '../../v1-parameters.yaml#/limitParam'
  responses:
    '308':
      $ref: 'v1-response-find-names.yaml'
